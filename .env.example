# Supabase — pre-populated automatically, do not change
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Edge function secrets — configured via Supabase dashboard or MCP tools
# These are NOT exposed to the frontend. They are read by the edge function at runtime.

# The email address where contact form notifications are sent
ADMIN_EMAIL=manigandan3408@gmail.com

# Resend API key for sending transactional emails
# Get one at https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
